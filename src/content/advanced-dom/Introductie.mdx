In deze module gaan we dieper in op de DOM. We zullen leren hoe we de DOM nog meer kunnen manipuleren en hoe we elementen kunnen toevoegen en verwijderen. We beginnen met een korte herhaling:

---

## Wat is de DOM?

De DOM, of Document Object Model, is een object georiënteerde representatie van een HTML document. Het is een boomstructuur van nodes die elk een deel van het document voorstellen. De DOM is een manier om een HTML document te representeren zodat we het kunnen manipuleren met JavaScript.

De DOM bestaat uit verschillende soorten nodes. Een node kan het volgende zijn:

- **Document**: de top node in de DOM-tree. Het heeft het volledige document in zich.
- **Element nodes**: Elke HTML-tag is een element in de boom.
- **Text nodes**: De tekstinhoud van een bepaald element is altijd een text-node. Witruimte wordt ook voorgesteld als een text-node!
- **Attribute nodes**: Een attribuut van een node is bereikbaar via het element, maar wordt niet in de boomstructuur getekend.
